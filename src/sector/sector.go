package sector

// Load loads the given sectors map data.
func Load(sec *Sector) error {
	// load the sector by sec.UID from some datasource
	return nil
}

func Update(sec *Sector) {
	for {
		// update players
		// update actors
		// check for collections
		// etc.
	}
}
